// src/tests/Example.spec.ts

describe('Example', () => {
    it('should do something', () => {
      // Defina o comportamento esperado e execute as verificações aqui
      expect(2 + 2).toEqual(4);
    });
  });
  